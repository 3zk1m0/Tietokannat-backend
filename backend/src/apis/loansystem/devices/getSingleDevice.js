import mysql from 'mysql2/promise';
import { connectionSettings } from '../../../settings';

// DELETE /resource/:id
async function getSingleDevices(ctx) {
  return 'Get Single Devices';
}

module.exports = getSingleDevices;
