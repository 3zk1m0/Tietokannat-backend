import mysql from 'mysql2/promise';
import { connectionSettings } from '../../../settings';

// DELETE /resource/:id
async function getSingleUsers(ctx) {
  return 'Get Single Users';
}

module.exports = getSingleUsers;
